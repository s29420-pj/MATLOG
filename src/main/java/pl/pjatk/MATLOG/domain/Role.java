package pl.pjatk.MATLOG.domain;

/**
 * Enum that represents available roles in application.
 * It's created for ease of creation objects with proper concrete class.
 */
public enum Role {
    STUDENT, TUTOR
}
